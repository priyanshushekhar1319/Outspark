services:
  - type: web
    name: outspark-backend
    env: python
    rootDir: backend
    startCommand: uvicorn main:app --host 0.0.0.0 --port 
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true